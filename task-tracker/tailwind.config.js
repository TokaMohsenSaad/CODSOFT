/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // For JavaScript, JSX, TypeScript, and TSX files
    "./public/index.html", // If you're using an HTML file, ensure it's included as well
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
